apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitness-app-chart.fullname" . }}
  labels:
    {{- include "fitness-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fitness-app-chart.selectorLabels" . | nindent 4 }}
